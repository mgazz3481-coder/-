<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Clicker Site</title>
    <style>
        /* Центрируем всё на странице */
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        /* Стили для надписи */
        .title {
            font-size: 24px;
            margin-bottom: 20px;
            color: #333;
        }

        /* Стили для картинки */
        .clickable-img {
            width: 300px; /* Можно настроить размер */
            cursor: pointer;
            transition: transform 0.1s;
            user-select: none;
        }

        /* Эффект нажатия */
        .clickable-img:active {
            transform: scale(0.95);
        }
    </style>
</head>
<body>

    <div class="title">Пиздани меня!</div>

    <img src="123.png" alt="Въеби тут" class="clickable-img" id="clickImage">

    <audio id="clickSound" src="333.mp3" preload="auto"></audio>

    <script>
        const image = document.getElementById('clickImage');
        const sound = document.getElementById('clickSound');

        image.addEventListener('click', () => {
            // Сбрасываем звук в начало, чтобы можно было кликать быстро подряд
            sound.currentTime = 0;
            sound.play();
        });
    </script>

</body>
</html>